#include "CyclicSortedList.h"
#include <stdint.h> //must have for uint32_t
#include <string> //must have for string

namespace study
{
    int ShortestWordDistance(std::string filename, std::string A="", std::string B="");
    bool isMatch(const char*, const char*);
    int singleNumber(int [], int);
    void removeDuplicateBitMap(std::string& str);
    void removeDuplicateHashMap(std::string& str);
    bool isAnagram(const std::string& str1, const std::string& str2);
    void rotate90(uint32_t* im, int N);
    int countUgly(std::string& s);
}
